{
  "framework": "vite",
  "installCommand": "cd client && yarn install",
  "buildCommand": "cd client && yarn build",
  "outputDirectory": "client/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
